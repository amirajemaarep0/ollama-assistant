# Interface
streamlit

# Context handling / RAG (LangChain + Chroma)
langchain
langchain-community
langchain-huggingface      # all-MiniLM-L6-v2 embeddings for the vector store
sentence-transformers      # required by langchain-huggingface
chromadb
pypdf                      # required by PyPDFLoader

# AI engine (local): Ollama
ollama
requests

# Transcription (local): OpenAI Whisper
openai-whisper
torch                      # required by whisper
sounddevice
numpy
scipy

# Code checking. Both are optional: the syntax check falls back to Python's own
# parser alone without them.
pyflakes                   # undefined names, unused imports
parso                      # recovers after a syntax error to find later ones

# Tests
pytest

reportlab
